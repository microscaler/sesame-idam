# Seed data for org-mgmt

SQL seed files for local development and testing.

Files are named with a 14-digit timestamp prefix for ordering.
Use `cargo run -p <migrator>` to discover and order these files.
