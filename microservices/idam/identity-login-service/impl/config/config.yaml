# BRRTRouter application configuration (YAML)
# Adjust values per environment and reload/restart the app.

security:
  jwks:
    BearerAuth:
      jwks_url: "http://identity-session-service:8080/idam/v1/.well-known/jwks.json"
      iss: "https://idam.example.com"
      aud: "sesame-idam"
      leeway_secs: 60
      cache_ttl_secs: 300
  api_keys:
    ApiKeyHeader:
      key: "test123"
    PlatformServiceAuth:
      key: "dev-platform-admin"
http:
  keep_alive: true
  timeout_secs: 5
  max_requests: 5000

# Browser / Swagger "Try it out": without a cors section, BRRTRouter still installs CORS
# middleware with an empty origin list and returns 403 for cross-origin Origin headers.
# Wildcard is dev-only (insecure for production); replace with explicit origins before prod.
cors:
  origins:
    - "*"
  allowed_headers:
    - "Content-Type"
    - "Authorization"
    - "Accept"
    - "X-API-Key"
    - "X-Platform-Admin-Key"
  allowed_methods:
    - "GET"
    - "POST"
    - "PUT"
    - "DELETE"
    - "OPTIONS"
    - "PATCH"
  allow_credentials: false
