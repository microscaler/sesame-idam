# Sesame frontend: sesame-tenant → https://sesame-tenant.dev.microscaler.local  (ADR-010)
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: sesame-tenant
spec:
  interval: 5m
  timeout: 5m
  releaseName: sesame-tenant
  targetNamespace: sesame-idam
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
      remediateLastFailure: true
  chart:
    spec:
      chart: ./helm/sesame-idam-frontend
      reconcileStrategy: Revision
      sourceRef:
        kind: GitRepository
        name: product-sesame-idam
        namespace: flux-system
      interval: 1m
  values:
    service:
      name: sesame-tenant
    image:
      name: sesame-idam-frontend-tenant
      tag: tilt # {"$imagepolicy": "flux-system:sesame-idam-frontend-tenant:tag"}
    route:
      enabled: true
      hostnames:
        - sesame-tenant.dev.microscaler.local

